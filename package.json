{
  "name": "tiles",
  "version": "0.1.0",
  "description": "A simple game of matching tiles built using create-react-app",
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.8.5",
    "@kadira/storybook": "^2.21.0"
  },
  "keywords": [
    "react",
    "create-react-app",
    "sample",
    "tiles"
  ],
  "author": "Sahil Jain (sahiljain112@gmail.com)",
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "repository": "https://github.com/sahiljain112/react-matching-tiles",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gh-pages -d build",
    "storybook": "start-storybook -p 9009 -s public",
    "build-storybook": "build-storybook -s public"
  }
}
